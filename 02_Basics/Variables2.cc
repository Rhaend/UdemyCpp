#include <iostream>

int main()
{

    // 1 Byte
    bool my_bool1 = true,
    bool my_bbol2 = false;

    // 1 Byte -128 .... +127 (2^8 = 256)
    char my_char = 10;

    // 2 Byte = 16 Bit (2^16 ~ 65.000)
    short my short = 300;

    // 4 Byte = 32 Bit (2^32 ~ 4.000.000.000)
    int my_int = 100'000;

    // 8 BYte 64 bit (2^ 64)
    long long my_11

    // 4 Byte = 32 bit
    float my_float = 10.0;

    // 8 BYte 64 bit
    double my_dbl = 10.0;



    return 0;
}
